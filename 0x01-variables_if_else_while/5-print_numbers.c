#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	unsigned int index = 0;

	for(index = 0; index <= 9; index++)
		printf("%d", index);
	printf("\n");
	return (0);
}
